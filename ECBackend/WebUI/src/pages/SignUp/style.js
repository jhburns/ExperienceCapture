import styled from 'styled-components';

const Wrapper = styled.section`
  margin-top: 15vh;
`;

const Logo = styled.img`
  width: 6rem;
  height: auto;
  margin: auto;
  display: block;
`;

export { Wrapper, Logo, };