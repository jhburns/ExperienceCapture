http://{$aws_domain_name} {
	redir https://{host}{uri}
}

https://{$aws_domain_name} {
	gzip

	log stdout
	errors stdout

	proxy /api/v1 api:80 {
		without /api/v1
		transparent
	}

	proxy / web:2015 {
		transparent
	}
}