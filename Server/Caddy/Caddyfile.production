# TODO: fix ACME SSL

http://{$aws_domain_name} {
	redir https://{host}{uri}
}

https://{$aws_domain_name} {
	log stdout
	errors stdout

	proxy /api/v1 api:80 {
		without /api/v1
		transparent
	}

	proxy / web:2015 {
		transparent
	}
}

# Healthcheck

:8080 {
    status 200 /
}